package interfaccestandard;

import java.util.Comparator;

// Arrays.sort(new Persona[10], new OrdinatorePersone());
public class OrdinatorePersone implements Comparator<Persona> {
	public int compare(Persona a, Persona b) {
		return 1;
	}
}
