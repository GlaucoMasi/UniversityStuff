Per garantire protezione, molte architetture di CPU prevedono più **modi di funzionamento** (**ring** di protezione).

**Dual Mode Architecture:**
- user mode
- kernel mode (anche supervisor o monitor mode)

**Realizzazione:** l'architettura hardware della CPU prevede almeno un bit di modo.